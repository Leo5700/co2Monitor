git clone https://github.com/...
cd numchar

git config --global user.email "you@mail.com"
git config --global user.name "Your Name"

git pull origin
git add .
git commit -a -m "my comment" (--amend)
git push -u origin master (--force)

# git push origin HEAD
